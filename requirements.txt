# Runtime del Automation Launcher.
#
# Compatible con Python 3.7 (kernel Athena). Los pines de version son
# los ultimos que soportan 3.7; en 3.8+ pip resolvera versiones mas nuevas.
ipywidgets>=7.5,<9
requests>=2.22
voila>=0.2

# Le da a Voila un kernel de Python al que conectarse. En el kernel Athena
# de produccion ya existe (por eso "doctor" solo lo reporta, no lo instala);
# en una maquina local nueva falta, y sin el Voila responde 500 con
# "No Jupyter kernel for language 'python' found".
ipykernel>=5.5,<6.10
